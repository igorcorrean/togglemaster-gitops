name: 🚀 GitOps Pipeline - Auth Service (Build, Update & Sync)

on:
  push:
    branches: [ main ]
    paths:
      - 'app/**'
      - 'Dockerfile'
      - 'go.mod'
      - 'go.sum'
      - '**.go'
  workflow_dispatch:

permissions:
  contents: write

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: auth-service
  OVERLAY_PATH: apps/overlays/production # ajuste para o caminho do overlay no seu repo (ex: overlays/dev ou apps/...)
  ARGOCD_APP_NAME: 'auth-service'

jobs:
  # ============================================
  # JOB 1: Build e Push da Imagem Docker
  # ============================================
  build-and-push:
    name: 🐳 Build and Push Docker Image
    runs-on: ubuntu-latest
    outputs:
      image-tag: ${{ steps.set-tag.outputs.tag }}
      ecr-registry: ${{ steps.login-ecr.outputs.registry }}
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
      
      - name: 🔑 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }} # Opcional se usar chaves de usuário IAM direto
          aws-region: ${{ env.AWS_REGION }}
      
      - name: 🔐 Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      
      - name: 🏷️ Set image tag
        id: set-tag
        run: echo "tag=v1.0.0-${{ github.sha }}-${{ github.run_number }}" >> $GITHUB_OUTPUT
      
      - name: 🐳 Build, tag, and push image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ steps.set-tag.outputs.tag }}
        run: |
          echo "🔨 Building Docker image..."
          docker build --platform linux/amd64 -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .
          
          echo "📤 Pushing to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest
          
          echo "✅ Image pushed: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"
      
      - name: 📊 Build Summary
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ steps.set-tag.outputs.tag }}
        run: |
          echo "## 🐳 Docker Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG\`" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Built and Pushed to ECR" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # JOB 2: Atualizar Manifests GitOps
  # ============================================
  update-gitops:
    name: 📝 Update GitOps Manifests
    runs-on: ubuntu-latest
    needs: build-and-push
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 🔧 Setup Kustomize
        run: |
          curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
          sudo mv kustomize /usr/local/bin/
          kustomize version
      
      - name: 📝 Update image tag in kustomization
        env:
          ECR_REGISTRY: ${{ needs.build-and-push.outputs.ecr-registry }}
          IMAGE_TAG: ${{ needs.build-and-push.outputs.image-tag }}
        run: |
          cd ${{ env.OVERLAY_PATH }}
          
          echo "🔄 Updating image tag to: $IMAGE_TAG"
          kustomize edit set image \
            auth-service=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          
          echo "✅ Kustomization updated"
          cat kustomization.yaml
      
      - name: 💾 Commit and push changes
        env:
          IMAGE_TAG: ${{ needs.build-and-push.outputs.image-tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          git add ${{ env.OVERLAY_PATH }}/kustomization.yaml
          
          if git diff --staged --quiet; then
            echo "⚠️ No changes to commit"
          else
            git commit -m "chore(gitops): update auth-service image to $IMAGE_TAG [skip ci]"
            git push origin main
            echo "✅ Changes pushed to repository"
          fi
      
      - name: 📊 GitOps Summary
        env:
          IMAGE_TAG: ${{ needs.build-and-push.outputs.image-tag }}
        run: |
          echo "## 📝 GitOps Update Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Target Service:** auth-service" >> $GITHUB_STEP_SUMMARY
          echo "**New Image Tag:** \`$IMAGE_TAG\`" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Manifests Updated and Committed" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # JOB 3: Validar Manifests
  # ============================================
  validate-manifests:
    name: ✅ Validate Manifests
    runs-on: ubuntu-latest
    needs: update-gitops
    
    steps:
      - name: 📥 Checkout código
        uses: actions/checkout@v4
        with:
          ref: main
      
      - name: 🔧 Setup Kustomize
        run: |
          curl -s "https://raw.githubusercontent.com/kubernetes-sigs/kustomize/master/hack/install_kustomize.sh" | bash
          sudo mv kustomize /usr/local/bin/
      
      - name: ✅ Validate Kubernetes manifests
        run: |
          echo "🔍 Validating manifests..."
          cd ${{ env.OVERLAY_PATH }}
          
          kustomize build . > /tmp/manifests.yaml
          
          echo "✅ Manifests are valid"
      
      - name: 📊 Validation Summary
        run: |
          echo "## ✅ Validation Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** ✅ Manifests Validated" >> $GITHUB_STEP_SUMMARY
          echo "**Note:** ArgoCD will auto-sync according to its polling interval" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # JOB 4: Pipeline Summary
  # ============================================
  pipeline-summary:
    name: 📊 Pipeline Summary
    runs-on: ubuntu-latest
    needs: [build-and-push, update-gitops, validate-manifests]
    if: always()
    
    steps:
      - name: 📊 Complete Pipeline Summary
        env:
          IMAGE_TAG: ${{ needs.build-and-push.outputs.image-tag }}
          ECR_REGISTRY: ${{ needs.build-and-push.outputs.ecr-registry }}
        run: |
          echo "## 🚀 GitOps Pipeline Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Pipeline Steps:" >> $GITHUB_STEP_SUMMARY
          echo "1. 🐳 **Build & Push:** ✅ Image built and pushed to ECR" >> $GITHUB_STEP_SUMMARY
          echo "2. 📝 **Update GitOps:** ✅ Manifests updated in Git" >> $GITHUB_STEP_SUMMARY
          echo "3. ✅ **Validate:** ✅ Manifests validated with Kustomize" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Deployment Details:" >> $GITHUB_STEP_SUMMARY
          echo "- **Image:** \`$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG\`" >> $GITHUB_STEP_SUMMARY
          echo "- **App Name:** ${{ env.ARGOCD_APP_NAME }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** 🎉 **READY - ArgoCD will deploy automatically**" >> $GITHUB_STEP_SUMMARY